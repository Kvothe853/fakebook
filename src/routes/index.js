const auth = require("./auth");
const questions = require("./questions");
const comments = require("./comments");
const users = require("./users");

module.exports = { auth, questions, comments, users };
